name: Airbnb Property Auto-Matching V1 Certification

on:
  pull_request:
    branches:
      - main
    paths:
      - 'src/api/airbnbHostSelfService.ts'
      - 'src/pages/distribution/AirbnbAutomaticListingDiscovery.contract.test.js'
      - 'src/pages/distribution/ConnectionCenterPage.tsx'
      - '.github/workflows/airbnb-automatic-listing-discovery.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  airbnb-property-auto-matching:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout exact candidate
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Use Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm

      - name: Install locked dependencies without lifecycle scripts
        run: npm ci --ignore-scripts --no-audit --no-fund

      - name: Run Airbnb auto-matching contracts
        run: node --test src/pages/distribution/AirbnbAutomaticListingDiscovery.contract.test.js

      - name: Build dashboard without deployment
        run: npm run build

      - name: Prove authorized diff allowlist
        shell: bash
        run: |
          git fetch origin main --depth=1
          git diff --name-only origin/main...HEAD | sort > "$RUNNER_TEMP/actual.txt"
          cat > "$RUNNER_TEMP/allowed.txt" <<'EOF'
          .github/workflows/airbnb-automatic-listing-discovery.yml
          src/api/airbnbHostSelfService.ts
          src/pages/distribution/AirbnbAutomaticListingDiscovery.contract.test.js
          src/pages/distribution/ConnectionCenterPage.tsx
          EOF
          sort "$RUNNER_TEMP/allowed.txt" -o "$RUNNER_TEMP/allowed.txt"
          unexpected="$(comm -23 "$RUNNER_TEMP/actual.txt" "$RUNNER_TEMP/allowed.txt")"
          if [ -n "$unexpected" ]; then
            printf 'Unexpected files outside Airbnb auto-matching surface:\n%s\n' "$unexpected"
            exit 1
          fi

      - name: Prove callback router shell and frame policy remain frozen
        run: |
          git diff --exit-code origin/main...HEAD -- \
            src/pages/distribution/AirbnbConnectionCallbackPage.tsx \
            src/app/routes/router.tsx \
            src/app/layout/AppShell.tsx \
            src/lib/distributionFramePolicy.ts \
            vercel.json

      - name: Prove one read-only discovery call and no portfolio rendering
        run: |
          test "$(grep -c 'listAirbnbHostListings(id)' src/pages/distribution/ConnectionCenterPage.tsx)" -eq 1
          ! grep -E 'listings\.map\(' src/pages/distribution/ConnectionCenterPage.tsx
          ! grep -E 'setInterval|setTimeout|requestAnimationFrame' src/pages/distribution/ConnectionCenterPage.tsx

      - name: Prove auto-matching stops before mapping activation and reservation import
        run: |
          ! grep -E '/mappings|/activate|load_future_reservations|Map listing|Activate channel|Load reservations' \
            src/api/airbnbHostSelfService.ts \
            src/pages/distribution/ConnectionCenterPage.tsx

      - name: Prove no direct Channex access was introduced
        run: |
          ! grep -E 'app\.channex\.io|staging\.channex\.io|user-api-key' \
            src/api/airbnbHostSelfService.ts \
            src/pages/distribution/ConnectionCenterPage.tsx
